# TodoAPI

```
dotnet run --urls http://+:8000
docker run -it -p 8080:8080 todoapi:latest
dotnet publish --os linux --arch x64 /t:PublishContainer -c Release
curl localhost:32769/WeatherForecast -H 'accept: text/plain'
```

